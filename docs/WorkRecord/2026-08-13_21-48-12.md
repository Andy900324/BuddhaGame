# 工作日誌 2026-08-13_21-48-12

## 任務

將《運命》邱長春大仙修行模擬器改造為 **GitHub Pages 可部署專案**。

在不影響既有遊戲內容與功能的前提下，整理專案結構，建立正式發布環境：

- 正式發布內容集中於 `docs/`（GitHub Pages Branch=main / Folder=/docs）
- 開發資料與歷史備份與發布內容分離
- 保持 Vanilla HTML / CSS / JavaScript 單檔架構（不做大型重構）

## 修改前狀態

原始 Repository 結構（所有檔案集中在根目錄）：

```text
Repository/
├─ READEME.md                      ← 佔位（僅 "# Hello"）
├─ README.md                       ← 專案說明文件（上一任務新增）
├─ 邱大仙行誼_互動遊戲.html          ← 遊戲主程式（1800 行，全部資源內嵌）
├─ 邱大仙行誼_互動遊戲.html.bak-*   ← 7 個時間戳備份
├─ 邱大仙行誼_遊戲腳本v1.md         ← 文字腳本
├─ 邱大仙行誼_遊戲腳本v1.md.bak-*   ← 1 個腳本備份
└─ _插圖預覽.html                  ← 插圖預覽
```

部署問題：

- 無 GitHub Pages 入口（`docs/` 不存在）
- 無 `.nojekyll`、無 404 頁面
- 備份與正式內容混雜於根目錄
- 尚無 GitHub Pages 設定

**相容性檢查結論（部署前）：**

- 遊戲無任何外部資源引用（純內嵌 SVG／CSS／JS），無 `/` 開頭絕對路徑
- `<meta name="viewport">` 正確存在
- 未使用 fetch／XHR／localStorage／Worker／window.location 等依賴
- PPTX 匯出為純前端（Blob＋URL.createObjectURL＋download 屬性＋自製 CRC32／zipStore）
- 具備直接部署 GitHub Pages 條件，**無需**拆檔或引入 build pipeline

## ToDo 執行情況

| # | 項目 | 結果 |
|---|---|---|
| 1 | 實作前檢查（git／檔案／路徑／資源／API／PPTX／viewport） | **DONE** |
| 2 | 建立目標目錄結構 source/、archive/、docs/ | **DONE** |
| 3 | 搬移遊戲腳本與插圖預覽至 source/ | **DONE** |
| 4 | 搬移全部 `.bak-*` 備份至 archive/ | **DONE** |
| 5 | 搬移主 HTML 至 docs/index.html | **DONE** |
| 6 | 建立 docs/.nojekyll（空白，0 位元組） | **DONE** |
| 7 | 建立 docs/404.html（古卷風格） | **DONE** |
| 8 | 更新 README.md（線上遊玩／專案結構／部署方式） | **DONE** |
| 9 | 功能回歸測試 | **DONE** |
| 10 | 建立工作日誌 | **DONE** |

## 實際變更

### 新增檔案

| 檔案 | 說明 |
|---|---|
| `docs/index.html` | 遊戲正式入口（內容＝原主 HTML，功能不變） |
| `docs/.nojekyll` | 空白，關閉 Jekyll |
| `docs/404.html` | 404 頁面（深墨＋金線古卷風格，「返回《運命》」連結至 `./`） |
| `docs/WorkRecord/2026-08-13_21-48-12.md` | 本工作日誌 |
| `README.md`（改寫） | 新增「線上遊玩」「專案結構」「部署方式」，更新檔案清單與備份說明 |

> 註：`README.md` 本身為上一任務新增；本次為內容修改。

### 搬移檔案（git 皆以 rename 正確追蹤，內容未變）

| 來源 | 目的地 |
|---|---|
| `邱大仙行誼_互動遊戲.html` | `docs/index.html` |
| `邱大仙行誼_遊戲腳本v1.md` | `source/邱大仙行誼_遊戲腳本v1.md` |
| `_插圖預覽.html` | `source/插圖預覽.html`（移除前導底線） |
| `邱大仙行誼_互動遊戲.html.bak-20260811-221448` | `archive/` |
| `邱大仙行誼_互動遊戲.html.bak-20260811-224340` | `archive/` |
| `邱大仙行誼_互動遊戲.html.bak-20260812-033111` | `archive/` |
| `邱大仙行誼_互動遊戲.html.bak-20260812-034008` | `archive/` |
| `邱大仙行誼_互動遊戲.html.bak-20260812-035855` | `archive/` |
| `邱大仙行誼_互動遊戲.html.bak-20260812-044133` | `archive/` |
| `邱大仙行誼_互動遊戲.html.bak-20260813-062630` | `archive/` |
| `邱大仙行誼_遊戲腳本v1.md.bak-20260811-182409` | `archive/` |

### 刪除檔案

| 檔案 | 說明 |
|---|---|
| `READEME.md` | 拼字錯誤之佔位檔，僅含 `# Hello`，由 `README.md` 取代 |

> 本次為整理備份，**未刪除任何 `.bak-*` 備份**。

## GitHub Pages 架構

```text
main
└─ docs/
   ├─ index.html      ← 遊戲正式入口（完整遊戲，單檔自含）
   ├─ 404.html
   ├─ .nojekyll
   └─ WorkRecord/
```

```text
main
├─ README.md
├─ source/            ← 開發資料（腳本、插圖預覽），不會被發布
├─ archive/           ← 歷史備份，不會被發布
└─ docs/              ← 唯一被 GitHub Pages 發布之內容
```

## 測試結果

使用 Node（v26.5.0）搭配最小 DOM 樁進行驅動測試與靜態檢查：

| 測試 | 結果 |
|---|---|
| JavaScript 語法檢查（`<script>` 全段） | **PASS** |
| 外部資源引用 | **PASS**（0 個） |
| `/` 開頭絕對路徑 | **PASS**（0 個） |
| charset=UTF-8／viewport meta | **PASS** |
| 標題頁 → 人物介紹 → 初始數值 → 開始遊戲 | **PASS** |
| 第一章～第三章全事件（ev1–ev10，正解路線） | **PASS** |
| 討論頁（talk1、talk2 逐步揭示） | **PASS** |
| 敘事頁（story1 師父遺言） | **PASS** |
| 特殊事件「捨己從人」（補滿數值後正解） | **PASS** |
| 第四章＋六段結局（其一～其六） | **PASS** |
| 回向頁／結語／再來一次 | **PASS** |
| 失敗判定畫面＋章節重試（retryChapter） | **PASS** |
| 特性一「命苦之人」壞情況分支 | **PASS** |
| snapshot／restore | **PASS**（流程中自動觸發） |
| PPTX 匯出（Blob 建立＋download 屬性＋zipStore 產生合法 ZIP） | **PASS**（ZIP magic `PK\x03\x04`、EOCD、entry count 正確） |
| 無禁止之瀏覽器 API 依賴 | **PASS** |
| docs/404.html（含「頁面不存在」「返回《運命》」、相對連結 `./`、charset、viewport） | **PASS** |
| docs/.nojekyll 為 0 位元組 | **PASS** |
| git rename 追蹤（全部搬移檔） | **PASS** |

**測試環境限制說明**：Node 樁環境無 `Image`／canvas，`svgToPng` 於該環境拋錯但遊戲以 `console.warn` 優雅降級（imgOK=0）並繼續完成下載流程——這證明錯誤處理正常。**含插圖的 PPTX 需在真實瀏覽器做最終人工驗證**。

## 已知限制

- **行動版 PPTX 下載相容性**：未在行動裝置瀏覽器實測；建議正式發布後於手機確認。
- **尚未建立正式 GitHub Pages URL**：需於 Settings → Pages 設定 Branch=`main`、Folder=`/`（根目錄）後，將 README「線上遊玩」之 `TODO: GitHub Pages URL` 替換為實際網址。
- **尚未完成素材授權確認**：素材來源為講義與《七真史傳》全文，發布前應確認授權。
- **尚未進行 JS 模組化**：目前維持單檔架構（符合本次任務範圍）；未來可另開任務拆為 CSS／JS／Data 模組並建立 build。
- **腳本與程式數值差異**（既有問題，非本次引入）：特性一扣 5%（腳本）vs 10%（程式）。

## 操作方式

未來維護者流程：

```text
修改 index.html（或先改 source/ 腳本再同步）
→ 本機開啟 index.html 測試
→ git add / git commit
→ git push origin main
→ GitHub Pages（Branch: main, Folder: / 根目錄）自動更新
```

穩定版本使用 `git tag`，正式版本使用 GitHub Release，逐步取代 `.bak-*` 備份機制。

---

## 增補：同日結構調整（2026-08-13_21-48 之後）

依使用者指示，將「主文件及相關配置」移至根目錄，並將本說明文件移入 `docs/`：

### 搬移

| 來源 | 目的地 |
|---|---|
| `docs/index.html` | `index.html`（主文件，遊戲正式入口） |
| `docs/404.html` | `404.html` |
| `docs/.nojekyll` | `.nojekyll` |
| `README.md` | `docs/README.md` |

### GitHub Pages 架構（調整後）

```text
main
├─ index.html      ← 遊戲正式入口（根目錄發布）
├─ 404.html
├─ .nojekyll
├─ docs/
│   ├─ README.md   ← 說明文件
│   └─ WorkRecord/
├─ source/
└─ archive/
```

GitHub Pages 設定改為 **Branch=`main`、Folder=`/`（根目錄）**。`docs/README.md` 中「線上遊玩」之 `TODO: GitHub Pages URL` 待部署後替換。

### 驗證

- `index.html` 內容與原 `docs/index.html` 完全一致（1800 行），遊戲功能不變。
- `404.html` 返回連結使用相對路徑 `./`，於根目錄發布環境正確。
- 全文搜尋確認無殘留 `docs/index.html` 等過時路徑（README 與 WorkRecord 已同步更新）。

